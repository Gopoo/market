package com.flyingstudio.fscore.ui.recycler;

/**
 * Created by guopu on 2017/10/19.
 */

public enum ItemFileds {
    /*
     *JSON数据的所有属性
     * 不直接将属性设为成员变量，而是作为HashMap的键值，将HashMap作为成员变量
     */
    ID,
    TYPE,
    PRICE,
    USER_NICK_NAME,
    USER_AVATAR,
    POST_TIME,
    IMAGE_URLS,
    CONTENT,
    BANNERS
}
